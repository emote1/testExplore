# Example environment variables for E2E tests
# Copy this file to .env (and/or .env.e2e) and fill the values.

# Reef EVM address used by Playwright tests
E2E_USER_ADDRESS=

# Optional: HTTP EVM JSON-RPC endpoint used to resolve tokenURI/uri for NFT metadata
# If not set or unavailable, the app will skip eth_call and show placeholders for some NFTs
# Example (replace with a provider that supports eth_chainId/eth_call):
# VITE_REEF_EVM_RPC_URL=https://<reef-evm-rpc>

# Optional: Reefscan base origin for UI links (no trailing slash)
# Defaults to http://reefscan.com
# VITE_REEFSCAN_ORIGIN=http://reefscan.com

# Optional: Override REEF price endpoint (defaults to CoinGecko)
# VITE_REEF_PRICE_URL=https://api.coingecko.com/api/v3/simple/price?ids=reef&vs_currencies=usd&include_24hr_change=true

# Optional: USD multiplier to adjust displayed fiat value (applied to price before caching)
# Example: 0.1 shifts the decimal one place left (10x smaller USD)
# VITE_REEF_USD_MULTIPLIER=0.1

# Historical USD (USD(H)) for Rewards chart
# By default the app requests CoinGecko daily prices endpoint:
#   https://api.coingecko.com/api/v3/coins/reef/market_chart?vs_currency=usd&days=365&interval=daily
# CoinGecko may require an API key (401 Unauthorized). Provide one of the keys below.
# PRO key (paid):
# VITE_COINGECKO_API_KEY=
# Demo key (free, rate-limited):
# VITE_COINGECKO_DEMO_KEY=
# Optional: use your own proxy/base URL for history (must be compatible with CoinGecko market_chart response)
# VITE_REEF_PRICE_HISTORY_URL_BASE=https://api.coingecko.com/api/v3/coins/reef/market_chart

# Optional: Limit concurrent prefetch batches across contracts (default 16)
# Reduce if you see too many parallel requests on slow networks; increase for faster endpoints
# VITE_PREFETCH_MAX_WORKERS=16

# Optional: Limit concurrent metadata fetch workers (default 12)
# Applies to the main metadata JSON fetch stage
# VITE_FETCH_CONCURRENCY=12

# Optional: IPFS gateway override(s)
# Single gateway (used when VITE_IPFS_GATEWAYS is empty). Must end with /ipfs/ or will be normalized.
# VITE_IPFS_GATEWAY=https://reef.infura-ipfs.io/ipfs/
# Multiple gateways, comma-separated. Left-to-right failover order. Takes precedence over VITE_IPFS_GATEWAY.
# VITE_IPFS_GATEWAYS=https://reef.infura-ipfs.io/ipfs/,https://ipfs.io/ipfs/,https://cloudflare-ipfs.com/ipfs/,https://dweb.link/ipfs/

# How to run E2E (comments only, keep .env as key=value lines):
# Запуск E2E:
# Обычный запуск:
# powershell
# pnpm test:e2e
# UI-режим:
# powershell
# pnpm test:e2e:ui
# В «головом» режиме:
# powershell
# pnpm test:e2e:headed
# Конкретный тест:
# powershell
# npx playwright test tests/e2e/nft.spec.ts
# По части названия:
# powershell
# npx playwright test -g "NFT Gallery"
# Открыть отчёт:
# powershell
# npx playwright show-report

# Optional: Enable Apollo timing logs (TTFB and response size) in dev
# Set to 1 or true to log timings for Transfers queries
# VITE_APOLLO_TIMING=1

# Aggregator API base URL (for pre-aggregated metrics: active wallets, sparklines, etc.)
# When empty or unset, the app uses local mock data from /public/mock-aggregator/
# Example: http://localhost:3001 or https://api.reef-aggregator.example
# VITE_AGGREGATOR_URL=

# ICP JSON endpoint for daily active wallets sparkline (DailyWalletsSparklineResponse)
# Example: https://<canister-id>.raw.icp0.io/active-wallets-daily.json
# VITE_ICP_ACTIVE_WALLETS_DAILY_URL=