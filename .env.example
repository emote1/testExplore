# Example environment variables for E2E tests
# Copy this file to .env (and/or .env.e2e) and fill the values.

# Reef EVM address used by Playwright tests
E2E_USER_ADDRESS=

# Optional: HTTP EVM JSON-RPC endpoint used to resolve tokenURI/uri for NFT metadata
# If not set or unavailable, the app will skip eth_call and show placeholders for some NFTs
# Example (replace with a provider that supports eth_chainId/eth_call):
# VITE_REEF_EVM_RPC_URL=https://<reef-evm-rpc>

# Optional: Limit concurrent prefetch batches across contracts (default 16)
# Reduce if you see too many parallel requests on slow networks; increase for faster endpoints
# VITE_PREFETCH_MAX_WORKERS=16

# Optional: Limit concurrent metadata fetch workers (default 12)
# Applies to the main metadata JSON fetch stage
# VITE_FETCH_CONCURRENCY=12

# Optional: IPFS gateway override(s)
# Single gateway (used when VITE_IPFS_GATEWAYS is empty). Must end with /ipfs/ or will be normalized.
# VITE_IPFS_GATEWAY=https://reef.infura-ipfs.io/ipfs/
# Multiple gateways, comma-separated. Left-to-right failover order. Takes precedence over VITE_IPFS_GATEWAY.
# VITE_IPFS_GATEWAYS=https://reef.infura-ipfs.io/ipfs/,https://ipfs.io/ipfs/,https://cloudflare-ipfs.com/ipfs/,https://dweb.link/ipfs/

# How to run E2E (comments only, keep .env as key=value lines):
# Запуск E2E:
# Обычный запуск:
# powershell
# pnpm test:e2e
# UI-режим:
# powershell
# pnpm test:e2e:ui
# В «головом» режиме:
# powershell
# pnpm test:e2e:headed
# Конкретный тест:
# powershell
# npx playwright test tests/e2e/nft.spec.ts
# По части названия:
# powershell
# npx playwright test -g "NFT Gallery"
# Открыть отчёт:
# powershell
# npx playwright show-report

# Optional: Enable Apollo timing logs (TTFB and response size) in dev
# Set to 1 or true to log timings for Transfers queries
# VITE_APOLLO_TIMING=1